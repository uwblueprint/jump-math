# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy frontend to Firebase Hosting prod environment

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"

defaults:
  run:
    working-directory: frontend

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "REACT_APP_BACKEND_URL=${{ secrets.PROD_BACKEND_URL }}" > .env
      - run: rm -rf node_modules && yarn install --frozen-lockfile && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_JUMP_MATH_98EDF }}"
          channelId: live
          projectId: "${{ secrets.DEV_PROJECT_ID }}"
          target: prod
          entryPoint: ./frontend
